Program Jenis_Akar

Deklarasi
    a, b, c, D : real

Deskripsi
    INPUT a, b, c
    D <- b^2 - 4*a*c
    
    IF D > 0 THEN
        OUTPUT "Akar Real dan Berlainan"
    ELSE IF D = 0 THEN
        OUTPUT "Akar Real dan Kembar"
    ELSE
        OUTPUT "Akar Imajiner"
